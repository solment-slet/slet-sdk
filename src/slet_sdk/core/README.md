`core/` используется для общих модулей которые использует пользователь и остальные модули.
`slet_sdk/schemas.py`, `slet_sdk/exceptions.py`... используются для удобного импорта для пользователя, без использования `core` в импортах.